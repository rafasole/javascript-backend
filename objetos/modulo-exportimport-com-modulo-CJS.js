function calculaSoma (numero1, numero2) { 
    const soma = numero1 + numero2;
    return soma
}

module.exports = calculaSoma;

//no modulo em cjs a saída para exportação precisa ser sempre via module.exports