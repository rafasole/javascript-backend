export const calculaSoma = (numero1, numero2) => { 
    const soma = numero1 + numero2;
    return soma
}
//lembrar de salvar o módulo sempre como ".mjs"
//passar o "export" na declaração da função
